import(DBI,RPostgreSQL,ggplot2,grid,plyr,reshape2,magrittr,methods)
importFrom(curl,curl)

export(
       bake,broil,
       getMLEs,recMLEs,
       recScript,listScripts,catScript,dropScript,
       getQuery,writeDBTable,
       startTunnel,stopTunnel,
       random.org,urandom,
       rngControl,rngSeeds,
       plotMatrix,
       scinot
       )

S3method(plotMatrix,list)
S3method(plotMatrix,data.frame)
S3method(print,aakplot)
